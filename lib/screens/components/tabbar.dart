import 'package:flutter/material.dart';

class TabBar extends StatelessWidget {
  const TabBar({Key? key, required List<Widget> tabs}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
